<nav class="navbar" style="background-color: #e3f2fd;">
    <nav class="navbar navbar-expand-lg ">
        <div class="container-fluid">
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav"> 
              <li class="nav-item">   
               <%= link_to  theater_path(session[:current_theater_id]), class: "nav-link active" do%>  
               <i class='bx bx-home text-primary' ></i>
               <% end %>
              </li> 
              <li class="nav-item">                  
                <%= link_to 'BookingInfos', booking_infos_path, class: "nav-link"%> 
              </li>
              <li class="nav-item">   
                <%= link_to ' Screens', screens_path, class: "nav-link" %> 
              </li>  
              <li class="nav-item">    
                <%= link_to 'Shows', shows_path, class: "nav-link" %>  
              </li>  
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                  <i class='bx bx-cog text-primary' ></i>
                </a> 
                <ul class="dropdown-menu">
                  <li class="dropdown-item" > 
                    <%= link_to  edit_password_path(email: current_user.email) do%>  
                    <i class='bx bxs-key text-info'></i> 
                    <% end %>
                  </li>
                  <li class="dropdown-item"> 
                    <%= link_to  destroy_user_session_path, method: :delete do%> 
                    <i class='bx bx-log-out text-primary'></i>        
                    <% end %>
                  </li> 
                </ul>
              </li>  
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                  Theaters 
                </a> 
                <ul class="dropdown-menu"> 
                  <% @theaters.each do |theater| %>
                    <li class="dropdown-item <%= 'active' if theater.id == session[:current_theater_id] %>">
                      <%= link_to theater.name, theater_path(theater.id), onclick: "setTheaterId(#{theater.id})" %>   
                    </li>
                  <% end %> 
                </ul>  
              </li>  
            </ul>  
            <a id="selected-theater">
              <%= "[" + @theaters.find_by(id: session[:current_theater_id]).name + "]" %>
            </a> 
          </div>
        </div>
    </nav> 
    <h4><%= current_user.email %></h4>    
</nav>  

 
  
                

